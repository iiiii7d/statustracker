services:
  postgres:
    image: docker.io/library/postgres:18.6
    environment:
      POSTGRES_DB: statustracker
      POSTGRES_USER: statustracker
      POSTGRES_PASSWORD: password
    volumes:
      - "pgdata:/var/lib/postgresql"
    ports:
      - "5432:5432"

volumes:
  pgdata:
